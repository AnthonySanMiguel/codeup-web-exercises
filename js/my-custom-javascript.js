"use strict";

$(document).ready(function() {

    // alert('The DOM has finished loading!'); // Will create an alert on page refresh once DOM has finished loading

    // alert($('#final_word').html()); // Will create an alert on page refresh that displays content of element with ID of "final_word"

    // $('.codeup').css('border', '1px solid red'); // Targets all elements with class of "codeup" and adds a 1px border red

    // $('li').css('font-size', '20px'); // Targets list item "li" elements and changes the font sizes for all

    // $('h1, p, li').css('background-color', 'red'); // Targets multiple element types and changes the background color for all

    // alert($('h1').html());; // Will create an alert on page refresh that displays content of h1 element

    // $('h1').css('background-color', 'red'); // Targets h1 element and changes the background color for all
    // $('p').css('background-color', 'yellow'); // Targets p element and changes the background color for all
    // $('li').css('background-color', 'green'); // Targets li element and changes the background color for all

    // $('li').css('font-size', '20px').css('border', '1px solid red'); // Targets list item "li" elements and changes the font sizes for all AND adds red border

    // $('h1').click(function() { // Will change the background color of all h1 elements when single-clicked
    //     $(this).css('background-color', 'red');
    // });
    //
    // $('p').dblclick(function() { // Will change the font size of all p elements when double-clicked
    //     $('p').css('font-size', '18px');
    // });
    //
    // $('li').hover( // Will change effect of li items to red when hovered over and black when not hovered over
    //     function() {
    //         $('li').css('color', 'red'); // <---- If 'li' target is set to 'this", it will target each specific element (e.g. THE ONE THAT IS CURRENTLY BEING INTERACTED WITH, OR 'HOVERED' OVER) rather than the entire block together as one interactive object.
    //     },
    //     function() {
    //         $('li').css('color', 'black');
    //     }
    // );

});
